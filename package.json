{
  "name": "winecalcs",
  "version": "1.0.0",
  "private": true,
  "description": "WineCalc Pro local web app",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p dist && cp index.html dist/index.html"
  }
}
